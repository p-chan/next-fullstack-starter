import { LogoutContainer } from '@client/components/Logout'
import { PageWrapperContainer } from '@client/components/shared/PageWrapper'

const Page = () => (
  <PageWrapperContainer>
    <LogoutContainer />
  </PageWrapperContainer>
)

export default Page
