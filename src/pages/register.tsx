import { RegisterContainer } from '@client/components/Register'
import { PageWrapperContainer } from '@client/components/shared/PageWrapper'

const Page = () => (
  <PageWrapperContainer>
    <RegisterContainer />
  </PageWrapperContainer>
)

export default Page
